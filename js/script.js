// window.addEventListener('scroll', function() {
//     const maxScroll = document.documentElement.scrollHeight - window.innerHeight;
//     const scrollTop = window.scrollY;
//     const scrollFraction = scrollTop / maxScroll;
//     const maxDegree = 360; // Maximum degree for the gradient
//     const degree = 45 - (scrollFraction * 45); // Change from 45deg to 0deg on scroll

//     document.body.style.backgroundImage = `linear-gradient(${degree}deg, #FF3CAC 0%, #784BA0 50%, #2B86C5 100%)`;
// });
